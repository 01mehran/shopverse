// Components;
import { BreadCrumb, Header, TopBar } from "@/shared/components";

export default function Cart() {
  return (
    <section>
      <TopBar />
      <Header />
      <BreadCrumb />
    </section>
  );
}
