// Components;
import { BreadCrumb, Header, TopBar } from "@/shared/components";

export default function ProductDetails() {
  return (
    <section>
      <TopBar />
      <Header />
      <BreadCrumb />
    </section>
  );
}
